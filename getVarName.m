%tengo que hacer una función que me de el nombre de las variables en string
%porque MATLAB no funciona como python y no puedo acceder a las propiedades
%de las variables así como así

function out = getVarName(var)
    out = inputname(1);
end